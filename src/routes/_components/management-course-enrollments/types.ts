/**
 * Shared types for the course-enrollment management screen, extracted from the
 * former 1k-line management-course-enrollments.tsx so the page shell, the three
 * tab sections and the hooks agree on one definition instead of re-declaring it.
 */

export type TabKey = "roster" | "bulk" | "codes";
